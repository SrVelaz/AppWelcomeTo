<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="250dp"
        android:layout_marginTop="10dp"
        android:id="@+id/imageView"
        android:src="@drawable/conjunto"
        android:layout_weight="2"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/scrollView"
        android:layout_marginLeft="15dp"
        android:layout_marginRight="15dp"
        android:layout_weight="2 ">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="
            Esta es una APP muy masculina realizada solamente para hombres de verdad. Mientras los buscamos conformence con estos dos sementales mal recortados.\n
             Rogamos discupen las molestias. 8====D"
            android:textSize="15sp"
            android:id="@+id/textView2"
            android:textColor="@android:color/holo_blue_dark"/>
    </ScrollView>

    <Button
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="Sal de aquí que esto no me interesa"
        android:layout_marginBottom="15dp"
        android:gravity="center_horizontal"
        android:id="@+id/botonVolverAlInicio_info"
        android:onClick="pulsarBotonInfo_volver"/>
</LinearLayout>