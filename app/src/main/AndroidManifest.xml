<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.srvelaz.appwelcometo" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EncuestaActivity"
            android:label="Encuesta activity"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".EncuestaActivity1"
            android:label="Encuesta activity 1"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".EncuestaActivity2"
            android:label="Encuesta activity 2"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".UnidadesActivity"
            android:label="Unidades Activity"
            android:theme="@style/Theme.AppCompat"></activity>
        <activity android:name=".InfoActivity"
            android:label="Info Activity"
            android:theme="@style/Theme.AppCompat"></activity>
    </application>

</manifest>
